# .agents/scripts/

Global scripts declared in `aiconf.manifest.json` live here.

Each script must have an explicit command name, target under `~/.local/bin`, and executable mode in the manifest. `aiconf` never infers commands from every file in this directory.
